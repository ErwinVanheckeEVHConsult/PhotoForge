# Planning Proposal  — v000.007

Starting planning proposal.

---

## Context

- Previous versions are complete and released
- All existing behavior is frozen and must not change
- Scope must come strictly from ProjectDocs/backlog.md
- No changes to existing architecture unless explicitly required

---

## Candidate Scope (from backlog)

- Full processing support for non-JPEG formats
- TIFF support
- Use filename patterns as fallback timestamp source when EXIF is missing
- Use folder name patterns as fallback timestamp source when EXIF is missing
- Detect inconsistent timestamp clusters
- Extract metadata from filenames
- Extract metadata from folder names
- Expose EXIF diagnostics (missing, unreadable, invalid fields)
- Support EXIF timezone fields if present
- Align EXIF module placement with metadata_extractors structure

---

## Goal

Make metadata a fully integrated, deterministic, and format-agnostic foundation of the pipeline, enabling consistent processing across all supported file types without altering existing behavior.

---

## Responsibilities

- Define what must be delivered
- Keep scope aligned with backlog
- Avoid redesign or feature creep

---

## Non-Goals

- No changes to existing runtime behavior
- No architectural redesign
- No speculative or future-facing features

---

## Deliverables

- Deterministic metadata resolution logic applied across all processed files
- Extended pipeline supporting non-JPEG formats end-to-end, including TIFF
- Explicit timestamp fallback hierarchy (EXIF → filename → folder → filesystem)
- EXIF timezone-aware timestamp resolution when timezone data is available
- Deterministic metadata diagnostics output, including:
  - missing metadata
  - unreadable or invalid EXIF fields
  - inconsistent timestamp clusters
- EXIF module placement aligned with metadata_extractors structure
- Updated documentation reflecting metadata behavior and rules

---

## Constraints

- No modification of existing validated behavior
- No new architecture
- No deviation from backlog-defined scope
- Deterministic and minimal changes only

---

## Acceptance Criteria

- Scope must come strictly from ProjectDocs/backlog.md
- No unintended side effects on existing behavior
- Deliverables are complete and minimal
- Structure remains consistent with prior versions

---

## Risks

- Scope creep
- Implicit redesign
- Hidden behavioral changes

---

## Success Definition

- Scope is clearly defined and minimal
- Work can proceed without ambiguity
- No impact on existing system behavior
