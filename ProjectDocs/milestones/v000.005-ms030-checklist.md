# Milestone Checklist — v000.005-ms030

---

## Completion

- `Contextual grouping computed from FileRecord set`
- `ContextualGrouping object produced as part of pipeline flow`
- `No changes to existing pipeline outputs or behavior`
- `No changes to PlanResult structure`

---

## Validation

- Contextual grouping is computed from the complete set of valid `FileRecord` objects
- Grouping uses only:
  - `timestamp`
  - `timestamp_source`
- Grouping follows the deterministic rules defined in MS029
- Contextual grouping is computed exactly once per pipeline execution
- Contextual grouping is fully deterministic for identical input
- Contextual grouping is computed after scanning and metadata normalization
- Contextual grouping does not modify or filter the `FileRecord` set
- Contextual grouping does not influence:
  - duplicate grouping (hash-based)
  - canonical selection
  - planner behavior
  - action generation
  - reporting output (existing formats)
- Contextual grouping is not used to:
  - change filenames
  - change folder structure
  - change grouping of duplicates
  - influence any existing decision logic
- Contextual grouping is invoked only by the internal orchestration function defined in this milestone
- Neither `scanner.py` nor planner logic computes contextual grouping directly
- Within the orchestration function, contextual grouping is computed once from the complete valid `tuple[FileRecord, ...]` and then propagated unchanged
- The orchestration function is implemented in `pipeline.py`
- The orchestration module is the only location where:
  - scanning
  - planning
  - contextual grouping
  are composed into a single flow
- `pipeline.py` does not implement:
  - scanning logic
  - planning logic
  - grouping rules
- `pipeline.py` only orchestrates calls to existing components
- Contextual grouping is produced as a separate `ContextualGrouping` object
- The grouping output is not embedded into:
  - `FileRecord`
  - `PlannedRecord`
  - `PlanResult`
- The orchestration interface:
  - consumes the existing valid `tuple[FileRecord, ...]`
  - invokes the existing planning flow unchanged
  - computes `ContextualGrouping` once from the complete valid `FileRecord` set
  - returns exactly:
    - `PlanResult`
    - `ContextualGrouping`
- CLI output remains unchanged in this milestone
- `scan_directory(...)` remains unchanged
- Existing planning entry points remain unchanged
- No additional fields are introduced in existing models
- Contextual grouping satisfies all structural invariants defined in MS028
- Contextual grouping satisfies all grouping rules defined in MS029
- For identical input:
  - identical `FileRecord` → identical contextual grouping
  - identical contextual grouping → identical `group_id` values
- Existing outputs remain strictly identical:
  - `records`
  - `actions`
  - `corrupt_files`
  - console output
  - JSON output
- No unintended side effects occur
- Deterministic behavior is preserved

---

## Scope

- No changes to scanner behavior
- No changes to hashing logic
- No changes to duplicate grouping
- No changes to canonical selection
- No changes to planner behavior
- No changes to reporter behavior
- No changes to CLI behavior
- No modification of `FileRecord`
- No modification of `PlannedRecord`
- No modification of `PlanResult`
- No integration into JSON or console output
- No metadata beyond allowed inputs
- No redesign introduced
- No contextual grouping logic is implemented directly inside `scanner.py`
- No contextual grouping logic is implemented directly inside planner decision logic
- No orchestration logic is implemented in:
  - `scanner.py`
  - planner implementation modules
  - CLI layer
- CLI invokes the orchestration function but does not implement orchestration logic

---

## Readiness

- Ready for integration into next version
- No blocking issues remain
