# Milestone Checklist — v000.005-ms027

---

## Completion

- `Metadata normalization integrated into scanner pipeline` is complete
- `Central normalization function used for all metadata entering the pipeline` is complete
- `No behavioral changes to existing JPEG processing` is confirmed
- `Pipeline prepared for multi-format metadata consumption` is complete

---

## Validation

- All metadata used to construct `FileRecord` passes through the central normalization function in `metadata.py`
- The normalization function is applied to all metadata used in `FileRecord` construction without exception
- All metadata used to construct `FileRecord` is obtained directly from the output of the central normalization function in `metadata.py`
- The normalization function is invoked from a single, clearly defined location in the scanner pipeline
- Metadata passes through the normalization function exactly once
- JPEG files still use the existing `extract_timestamp(...)` path unchanged
- For JPEG inputs, normalization acts as an identity mapping and does not transform `timestamp` or `timestamp_source`
- JPEG `timestamp` values remain identical to prior behavior
- JPEG `timestamp_source` values remain identical to prior behavior
- The normalization function does not raise new exceptions for valid JPEG inputs
- The normalization function does not suppress or alter existing exception behavior from upstream logic
- Normalized metadata values are not modified after normalization and before `FileRecord` construction
- No format-specific extraction logic was added to the scanner
- No non-JPEG extraction was integrated into the active pipeline
- Non-JPEG formats still do not produce `FileRecord` objects
- No additional metadata fields were introduced
- No implicit behavior is introduced
- No branching-based behavior changes are introduced outside defined integration rules
- No unintended side effects
- Deterministic behavior is preserved
- For identical JPEG-only input, the following remain identical:
  - `records` (content and ordering)
  - `skipped` (content and ordering)
  - `issues` (content and ordering)
  - `supported_files_processed`
  - `total_entries_seen`
- `FileRecord` objects constructed after integration are byte-for-byte equivalent to those produced prior to this milestone
- The normalization function accepts exactly:
  - `timestamp: datetime`
  - `timestamp_source: str`
- The normalization function returns a `NormalizedMetadata` object
- No tuple-based, dict-based, or alternative normalization return paths exist
- Values returned by `extract_timestamp(...)` are passed unchanged to the normalization function
- The normalization function is called exactly once per processed file
- No subsequent normalization calls on already normalized values occur
- For JPEG inputs:
  - `normalized.timestamp == input.timestamp`
  - `normalized.timestamp_source == input.timestamp_source`
- No reconstruction or transformation of values occurs
- `FileRecord` construction does not use raw values returned by `extract_timestamp(...)`
- `FileRecord` uses only values from the `NormalizedMetadata` object

---

## Scope

- Scope remains limited to pipeline metadata integration only
- No changes to hashing logic are introduced
- No changes to duplicate grouping are introduced
- No changes to planner behavior are introduced
- No changes to reporter behavior are introduced
- No modification of `FileRecord` fields or structure is introduced
- No modification of `extract_timestamp(...)` behavior is introduced
- No integration of non-JPEG extraction into the pipeline is introduced
- No pipeline restructuring is introduced
- No metadata inference, enrichment, or correction is introduced
- No additional metadata fields beyond `timestamp` and `timestamp_source` are introduced
- No redesign introduced

---

## Readiness

- Metadata normalization is consistently applied within the pipeline
- JPEG behavior remains strictly identical
- The pipeline remains deterministic and unchanged in behavior
- The system is ready for safe integration of non-JPEG formats in later milestones
- No blocking issues remain
