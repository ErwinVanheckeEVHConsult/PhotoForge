# Milestone Checklist — v000.005-ms025

---

## Completion

- `Format-specific metadata extraction for supported non-JPEG formats` is complete
- `Deterministic metadata extraction paths per format` are complete
- `Extraction functions produce values compatible with the normalized metadata structure` is complete
- `JPEG metadata extraction remains behaviorally identical` is complete
- `No changes to hashing, grouping, planning, or reporting behavior` is complete

---

## Validation

- Metadata extraction exists for PNG
- Metadata extraction exists for HEIC / HEIF
- Metadata extraction exists for RAW formats
- Metadata extraction exists for video formats
- Extraction output is limited to `timestamp` and `timestamp_source`
- Extraction behavior is deterministic for identical extraction inputs
- JPEG extraction still uses the existing `extract_timestamp(...)` path unchanged
- Format-specific metadata extraction is implemented but remains isolated and not integrated into the active pipeline
- No format-specific extraction logic was added to the metadata abstraction layer
- JPEG timestamp values remain identical for identical input
- JPEG timestamp_source values remain identical for identical input
- Non-JPEG extraction is not integrated into the active scanner pipeline
- Non-JPEG formats still do not produce `FileRecord` objects in the active pipeline
- No additional metadata fields were introduced
- No metadata inference or enrichment behavior was introduced
- No unintended side effects
- Deterministic behavior is preserved
- For each supported format, fallback to file modification time is explicitly implemented where defined
- When fallback is used, `timestamp_source` correctly reflects mtime usage
- `timestamp_source` values are consistent and deterministic across formats
- No scanner behavior changes are introduced
- No FileRecord generation for non-JPEG formats is introduced
- No pipeline restructuring is introduced
- Extraction functions remain unused by the active pipeline

---

## Scope

- Scope remains limited to defined milestone scope
- No redesign introduced

---

## Readiness

- Ready for integration into next version
- No blocking issues remain
